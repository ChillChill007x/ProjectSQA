# MOSA_EvoSuite / Configuration

Runner-managed artifacts: `<Project>/<Bug>/<run-id>/`.
Keep the complete run together; do not rename or edit evaluated Java files.

Read the root README.md, BENCHMARK_PROTOCOL.md and GIT-SETUP.md before submitting.
Empty directories are scaffolding, not experiment results.
